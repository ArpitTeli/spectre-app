services:
  - type: web
    name: spectre-relay
    runtime: node
    rootDir: relay-server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
